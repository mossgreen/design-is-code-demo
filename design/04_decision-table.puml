@startuml
' @package com.disc.decision
PricingService -> CustomerRepository: findTier(customerId)
PricingService <-- CustomerRepository: tier: Tier
PricingService -> DiscountCalculator: calculate(amount, tier)
PricingService <-- DiscountCalculator: finalPrice: BigDecimal
@enduml
